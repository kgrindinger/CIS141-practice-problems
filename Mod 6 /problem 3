# Create a list of strings. Write code to create a new list that includes only the strings
# longer than three characters. Print the resulting filtered list.

words = ["cab", "plane", "train", "moped", "car", "walk"]
print (words)
filtered_words = []
for word in words:
    if len(word) > 3:
        filtered_words.append(word)
print(filtered_words)
