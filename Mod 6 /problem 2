# Create a list of strings. Write code that counts how many times the word "Olympic" appears 
# in the list, and then print the count.

words = ["Olympic", "College", "Rangers", "Bremerton, WA", "Olympic", "Peninsula", "Olympic", "Games", "Medals"]
print (words)
count = 0
for word in words:
    if word == "Olympic" :
        count += 1
        
print (f"The word Olympic appears {count} times in the list.")
